# CodePet 저장소 지침

## 작업 진행 원칙

- 구현 요청은 명확한 범위에서 조사·수정·검증까지 이어간다. 일상적인 기술 선택이나 이미
  승인된 같은 작업을 반복 확인하지 않는다. 설명·리뷰만 요청했다면 임의로 구현하지 않는다.
- 새 비용·외부 공개·파괴적 작업·미정 제품 정책에만 필요한 확인을 한다. 보안·권한·개인정보·
  데이터 무결성·멱등성 경계는 유지하고, 한 부분이 막혀도 독립된 작업은 계속한다.
- 문서·지침만 바꾸면 내용·링크·명령·diff를 검증한다. 코드 빌드나 전체 테스트는 요구하지 않는다.
  국소 코드는 관련 정적 검사와 테스트, 공통 런타임·정식 릴리스는 필요한 전체 게이트를 실행한다.
- 새 테스트는 의미 있는 회귀를 잡을 때만 추가한다. 성공한 검증은 변경·실패로 무효화된
  범위만 다시 실행한다. DB·프로세스·외부 효과는 실제 통합 흐름으로 확인한다.
- 기존 미커밋 변경과 다른 작업자의 자원을 보존한다. 커밋·푸시는 승인 범위의 파일만
  명시해 수행하고, 실제 검증 결과와 미검증 범위를 구분해 간결한 한국어로 보고한다.
- 스킬·도구는 요청에 필요한 것만 사용한다. Superdesign·Orca는 사용자가 요청할 때만 쓴다.

## Git 반영

- 이 저장소의 원격 푸시는 `origin/main`에만 수행한다.
- 기능 브랜치나 다른 원격 브랜치를 생성하거나 푸시하지 않는다.
- 코드 주석은 기존 영어 관례를 유지하고, 커밋 제목은 한국어 Conventional Commits를 사용한다.

## 보고

- 커밋 제목과 푸시 결과는 한국어로 보고한다.
- 로컬 테스트, 패키징, 실행, 배포 상태를 서로 구분해 실제 확인한 범위만 말한다.
